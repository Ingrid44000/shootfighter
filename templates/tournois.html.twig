
<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">

</head>
<body>
<h1>Récompenses </h1>
{% block body %}


    {% for tournois in tournois %}

        {{ tournois.nom }} <br>
        {{ tournois.date | date ('d-m-Y') }}

         <img src="{{ asset('images/tournois/' ~ tournois.imageName) }}" alt="image goodie">
        <br>

        <button class="btn btn-link btn-secondary" type="submit">
            <a href="{{ path('app_inscription') }}" title = "inscription">S'inscrire</a>
        </button>
    {% endfor %}
{% endblock %}
</body>
</html>


